<form method="post" action="{{ path('app_kupac_delete', {'id': kupac.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kupac.id) }}">
    <button class="btn">Delete</button>
</form>
