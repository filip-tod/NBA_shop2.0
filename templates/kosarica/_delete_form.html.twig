<form method="post" action="{{ path('app_kosarica_delete', {'id': kosarica.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kosarica.id) }}">
    <button class="btn">Delete</button>
</form>
